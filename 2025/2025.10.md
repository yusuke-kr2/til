# 10月の目標
* カリキュラム基礎ステップまで全て終わらせる   
* 中間試験に取りかかる
* 現在作っているアプリを完成させる   
* Web技術入門、サーバーの基本を読了   

## 10/01(Wed)
*YWT*   
   
* Y   
・カリキュラムrails入門６   

* W   
・エラーによりどん詰まり。
理解が足りてなくてエラーになっているためカリキュラムを読み直し突破したい。   
db周りの内容だったかマイグレーションやコールバックなどの意味を理解できておらずカラムの追加等もうまくいかない時間が多かった。   

* T   
引き続きカリキュラムrails入門６    


## 2025/10/02(Thu)
*YMT*   
* Y   
・rails入門６〜８   
   
* W   
・６   
ストロングパラメーターの設定に漏れがあった。（def user_params）   
全てのデータを全部保存するのは危険ー＞悪意ある人はadmin=trueを送ったら管理者になれてしまうなど。   
それを防ぐためにpermitによりこのカラムだけを受け取ると宣言している。   
・７   
バリデーションにより空欄だとエラーにするなどその属性の値が要件を満たしているか検証できる。（必須入力、文字列の長さ、数値、範囲など）   
・８    
ログイン機能の付け方、セッションにより一時的に情報を保存する。   
resources,resourceベースでルーティングを設定。   
これを設定するとどんなルーティングが設定されるみたいなものがまだイメージついていない。   
   
* T   
・rails入門９   
長そうだが、終わればアプリ開発に一旦戻る。   


## 2025/10/03(Fri)
*YWT*   
* Y   
・rails入門９   
   
* W   
   resourceseによってget "ti/question1", to: 'ti#question1'のようなコードを省略できるらしい。   
   ただ、これはデータベースに保存させる場合で、クエリパラメーターを用いるときは上記のようにそのまま書く。   
   redirect_toで別ページへ飛ばす。   
   noticeでメッセージを残す。   
   ・rails入門は完了したが、理解できていないところも多い。この先アプリ作成やrails基礎を行う中でしっかり復習して理解していきたい。   

* T   
・キリがいいのもあるからJS入門を終わらせる。   
・アプリ開発再開   

## 20255/10/04(Sat)
*YWT*   

* Y   
・JS入門完了
・入門ステップ総復習   
   
* W   
   ・DOMはquerySelectorAllメソッドが文書内にある全ての＜p＞要素のリストを返さないといけないと定義している。   
   ・DOMはwebサイトを構築するために使用されるAPIの１つ   
   ・addEventListener（）→イベントを発行できるオブジェクトはこのメソッドを持っている。   
   ・イベントハンドラプロパティ→イベントを発生することができるオブジェクトは通常「on」にイベント名が続くプロパティを持っている(onclick)   
   →１つのイベントに複数のリスナーを追加できないデメリットもあり。   
   ・総復習でも忘れていることが多くあったので復習もして、アプリでさらに復習する。
      
* T   
   アプリ開発再開！   



## 2025/10/05(Sun)
*YWT*   
* Y   
データベース・SQL基礎１、２   
   
* W   
・E-R図：エンティティ、属性、リレーションを視覚的に表現   
   * エンティティ：システム内の重要なオブジェクトを表し、それぞれが一連の属性を持つ。   
   * 属性：エンティティに関連するデータ項目を表す   
   * リレーション：エンティティ間の関係性を示し、ビジネスルールを反映   
      
 ・トランザクション   
 全ての操作の完了or何も実行されていないのどちらかを保証してDBの整合性を保つ     
         
 ・インデックス設計   
 DBのデータに高速にアクセスする仕組み   
 *注意点：多用するとデータの挿入、更新、削除時にインデックスの更新が必要となりその分オーバーヘッドが発生。   
 シンプルにストレージ使用量も増加する。   

 ・気づいたらカリキュラムを進めていた。   


 * T   
 ・とりあえずカリキュラムデータベースとSQL基礎を終わらせてしまおう。       

 ## 2025/10/08(Wed)

 TIL入力サボりました。反省。   
    
*YWT*   
* Y   
・カリキュラムデータベースとSQL基礎完   
・カリキュラムruby基礎感   
   
* W   
・EXTRACT()   
日付（DATA型かTIMESTAMP型）から特定の要素を取り出す   
EXTRACT（取り出したい部分 from 日付列）   
・エイリアス   
テーブルに短い呼び名（エイリアス）を付け簡潔に書けるようにする。   
employees (as) e   
・group by    
組み合わせが同じ行を１つのグループにまとめる   
・order by   
指定した列の値を基準に検索結果を並び替える   

・combinationメソッド   
配列.combination(n).to_a    
to_aは配列に変換するメソッド   
(1..number).to_a.combination(2).to_a   
１からnumberまでの数の中から２つの数を組み合わせる   

・sort_data = array.sort{|a,b|b[1]<=>a[1]}   
arrayの中身を各要素の２番目の値（インデックス１）を基準に高順に並び替える（a[1]<=>b[1]なら昇順）   
{|a,b|...}   
sortの内訳では、要素を２つずつ取り出して、どちらを先にするかを決める   
その２つがブロック引数aとbに入る   


総じて入門ステップでやったことをしっかり忘れている。   
何度も書くがアプリを作ったりして復習を兼ねていきたい。   


* T   
カリキュラムjs基礎   


*おまけ*   
小さい内容でいいから技術記事も週１ぐらいで書いて行けたらいいと思った。   
sortについてなど小さいこと１つでもアウトプットを増やして少しづつでも定着させていけたら後々でかい。



## 2025/10/11(Sat)

*YWT*   
* Y   
カリキュラムjs基礎   

* W   
・cssやjsのコードを１から書くのは時間がかかるからpluginを導入することが多い。   
・querySelectorを使って要素を取得する。   
先頭ついている#はcssセレクタと呼ばれ、#(ID) .(クラス)  何もなし(htmlタグ)を指定する。   
    

* T   
アプリ作成   
rails基礎   


そろそろアプリも再開して、railsとはなんぞや、どうやって作っていくんだを理解していかなければならない。   
また、rails基礎については、自分で調べてコードを作っていけるようにカリキュラムが構成されているらしいからしっかりと自分で調べてクリアしていけるようにする



## 2025/10/12(Sun)
*YWT*   
* Y   
・カリキュラムrails基礎１、２   
・mainブランチで作業してしまいそのままmainでpushしてしまった場合の対処方   

* W    
・dockerでrailsアプリケーションを作成している場合rails gコマンドを使うことでgeneraiterを介してファイルを生成できる。   
・余計なファイルを生成しないようにする。   
・rubocopのLintチェック   
コードの品質やスタイルをチェックするツール。   
読みやすさ、保守性を向上させる。   
Gemfileにgem 'rubocop'を記述し、docker compose exec web bundle installをターミナルで実行すると使えるようになる。   
具体的にはインデントの間違い（rubyはインデント2個が標準）やコード中の無駄な空行や空白などを検知してくれる。   
・Asset pipeline   
railsのアセット（CSS,JS,画像等）を効率的に管理し配信する仕組み   
・mainブランチで作業してしまった場合はステージング前なら、普通にgit checkout で作業ブランチに移ってからステージングしたらいい。   
と思っていたがstashのがいい説が出てきて、記事読んでみたり同期と話してみたりしたが最終的に腑におちず。   
一旦諦めて進める。   
   
   ちなみにpushしてしまってから気づいた場合の対処方をここに残して、いつかの自分の助けにしたい。   
   ①p(作業ブランチ)にmainの内容を移す    
   ```  
   git checkout hoge   
   git merge main      
   ```
   ②mainを作業前に移す   
   ```   
   git checkout main   
   git log --oneline #作業前の正しい状態のコミットハッシュを取得   
   git reset --hard<作業前のコミットハッシュ>   
   #mainで作業する前の正しいコミットに戻す   
   ```
   ③正しい内容としてpushしなおす。   
   ```
   git push -f origin main   
   #-fを使う理由   
   #git pushだとリモートmainにある最新コミットよりもローカルmainの履歴が進んでいればpush,進んでいなければpushを拒否する仕組みの中で、この場合は後者であるため-fを使って強制的にpushしている。   
   ```
   ④pの内容を新しくpush   
   ```
   git push origin p   
   ```


   * T   
   カリキュラムrails3   


   この３はだいぶ時間がかかりそう。   
   これが終わったら一旦アプリに戻ります！


   ## 2025/10/14(Tue)
   *YWT*     

* Y   
・カリキュラムrails3   

* W   
・renderメソッド   
指定された部分テンプレートを現在のビューに埋め込むために使用される   
   　  
・Gemfile   
railsプロジェクトで使用するgem(ライブラリ)を管理するファイル   
プログラムに必要なすべてのgemが記述される   
   　   
・バリデーション   
入力されたデータが正しいかチェックする仕組み   
    
    ・ストリングパラメーター   
    railsデータを一括で更新する際に安全に更新するための仕組み   
    許可された属性のみを受け入れて不正な属性のデータベースへの更新を防ぐ   
       
* T   
引き続きrails3   


## 2025/10/15(Wed)
*YWT*   
* Y   
カリキュラムrails3   
   
   

* W
・`skip_before_action`   
コントローラーに特定のアクションが実行される前に設定されたフィルターをスキップするために使用   
```
skip_before_action :require_login, only: [:new, :create]
```
`require_login`というフィルターをnew,createアクションに対してスキップしている   
・`redirect_to`   
指定されたURLへユーザーをリダイレクトする   
リダイレクトはサーバーがクライアントに別のURLへの移動を指示するプロセス   
・レンダリング   
データを人間が見られるように変換して表示する処理   
ex) サーバーから取得したデータをもとに　　　
１、htmlを生成   
２、画面上に「記事一覧」や「プロフィール」などと表示   
この一連がレンダリング   
   
・`delete 'logout', to: 'user_sessions#destroy`   
ルーティング設定でログアウト機能を実装するために使用される記述   
   
・`if logged_in?`   
ユーザーがログインしているか判定し、真偽値を返す   
それに基づいて適切なヘッダー部分のテンプレートを表示する   
   
・routes.rbに記述する
```
get 'login', to 'hoge#hogehoge'   
post 'login', to 'fuga#fugafuga'   
delete 'logout', to 'piyo#piyopiyo'   
```
それぞれ`login_path`と`logout_path`が記述に応じて自動生成される   
   
・i18n   
様々な言語や地域設定に柔軟に対応できるように設定できる   
   
・YAML   
データを表現するためのフォーマット    
階層的なデータ構造をサポート   
情報を`キーと値`のペアで整理できる   
   
・`gem 'rails-i18n`   
Ruby on Railsアプリケーションを多言語対応させるためのライブラリ   
   
・`t('header.login')`
railsが用意しているtメソッド(translateメソッドのエイリアス)にheader-loginを渡すことで`config/locals/views/ja.yml`に記述された日本語`(ja/header/login)`を取得している   
   
* T   
アプリもやらなければいけないがカリキュラムが楽しいモードに入っているためカリキュラム続行！！   
アプリもちゃんとやるけども！！！   
   
### おまけ   
そういえばQiita更新した   
https://qiita.com/kurochannn/items/5dc2da562760663ce1cd


## 2025/10/16(Thu)
*YWT*   
* Y   
・カリキュラムrails4,5   
   
* W   
・`flash. each do |message_type, message|`   
`flash`に格納されているキー、バリューのセットをeachで処理している。   
その際、個々のキーバリューセットを`message_type`(キー)、`message`(バリュー)として扱い、以降の行でそれらを用いてHTMLを生成   
   
* T   
引き続きカリキュラムrails基礎   
初期の頃はなんだかんだ結局カリキュラムを終わらせることが目的になってしまっていて、アプリ作成でしか知識として固まっていかないと感じていたが、ここ最近カリキュラムを楽しめるようになり、進めながらしっかり一つ一つの意味を考えたり調べたりしながら進められるようになった。   
以前までもそうしていたつもりだが、早く進めなきゃという気持ちでやっていたため、あまり知識になっていなかった。   
今は逆に色々調べすぎて、カリキュラムの進度自体は悪くなっている気がするがそれでも今のが楽しい。   
   
でも1発では理解できないからいい塩梅を探して、進める、アプリで固める、を繰り返していきたい。


## 2025/10/17(Fri)
db系に苦手意識みたいなのがある。   
克服していきたい。   
*YWT*   
* Y   
・カリキュラムrails6   
   
* W   
・dreper   
railsのgemの１つ   
インストールするとdecoraterが作成できるようになる   
・decorater   
モデルに関連した元のオブジェクトを変更せずに新しいメソッドや属性を追加できるツール   
・`delegate_all`   
`decorater`クラスが元のオブジェクトの全てのメソッドを使えるようにするためのもの   
・   
```   
def full_name
  "#{object.last_name} #{object.first_name}"   
  ```   
  object = decoraterがラップしている元のオブジェクトのこと   
  `UserDecorater`の場合、objectはuserモデルのインスタンスを指す   
     
・`current_user`   
現在ログインしているユーザーの情報を取得するためのメソッド   
   
・リレーション   
データベースの異なるテーブル同士がどのように関連しているかを示すもの   
   
・アソシエーション   
railsのモデル間の関連性を定義する機能   
異なるモデル同士がどのように結びついているかを簡単に表現できる   
   
・バリデーション   
データがデータベースに保存される前にそのデータが正しい形式や条件を満たしているかを確認する   
   
・`@boards = Board.includes(:user)`  
　Boardモデルのレコードとそれに関連するuserモデルのレコードを一度に取得     
includesメソッドは関連するテーブルをまとめてDBから取得するメソッド     

と、まぁDB系ばっかやったわけだが、ここは今一番踏ん張りどころ。   
   
* T   
引き続きrailsカリキュラム   
DB系が続くから焦らず少しでも理解を増やしてやる。   

## 2025/10/18(Sta)
*YWT*   
* Y   
カリキュラムrails７、８   

* W   
・`config.time_zone = 'Tokyo'`    
デフォルトの文化ゾーンを日本時間に設定するための文化    
   
・db/seeds.rb   
開発環境やテスト環境で使用するデータベースの初期データを設定するファイル   
このファイルに記述された内容はrails db:seedコマンドを実行することでデータベースに適用される   
   
* T   
rails９   
ここから過去の内容やわからないことは自分で調べながらじゃないとクリア出来てなくなってくる。   
AIに丸投げではなく自分で解決する力を培っていく   
   

## 2025/10/19(Sun)
*YWT*   
* Y   
rails9   
   
* W   
・@boardテーブルには誰が掲示板を投稿したかの情報(user_id)が必要   
    
    ・タイピングミスが多いってのもあるけど、読み返した時にそれに気づけないのはまだ理解できていないからという部分も大きい。   
    ・前回やったことと少し違うだけで分からなくなってしまう。ざっくりと流れを掴むとこからスタートしてどこに何を追記したらいいかを考える。   
    終わってみればそんなことかっていうことも多々あるから流れを理解するところから再スタート！！   
       
* T   
カリキュラムrails10   

## 2025/10/20(Mon)   
*YWT*   
* Y   
rails10   
   
* W   
・`gem 'carrierwave'`    
ファイルのアップロードを簡単に行うためのライブラリ   
   
・`image_tag`   
画像を表示するためのヘルパーメソッド   
HTMLのimgタグを生成し指定された画像をページに表示されるために使う   
   
・`mount_uploader :board_image,BoardImageUploader`   
この記述を加えることでBoardモデルに対してcarrierwaveのアップローダークラス（BoardImageUploader）をマウントする   
マウント→モデルにアップローダーし画像ファイルを扱う機能をくっつけること   
   
・`extension_allowlist`   
carrierwave で許可されるファイル拡張子のリストを指定するために使用   
   
・`default_url`メソッド   
ファイルがアップロードされていない場合にデフォルトで表示する画像urlを指定   
   
・`file_field`   
ユーザーがファイルを選んでアップデートできるボタン   
   
・`hidden_field`   
ユーザーから見えないけどフォームに値を送るための隠し入力   
   
`form_with`   
フォームを作るための便利メソッド   
HTMLのフォームタグを自動で作ってくれる   
   
div class内に必要記述がなく永遠にエラー   
エラー内容とlocalhostを確認したらすぐわかったはずである   
エラー内容をよくみて対応していきたい   
基本中の基本である    
    
* T   
rails11   
   
## 2025/10/21(Tue)
*YWT*   
* Y   
・rails11   
   
* W   
・findメソッド   
渡されたIDを元にデータベースから特定のレコードを取得   
   
・find_byメソッド   
特定のカラムを検索対象として渡された値に最初にマッチした値を取得   
   
・whereメソッド   
find_byと違い検索対象全て   
   
・params   
コントローラーが呼ばれる時ブラウザからリクエストが送られてくる。   
そのとき、一緒に送られてくるデータをまとめて受け取るのがparams   
   
・orderメソッド   
データの並び順を指定する   
デフォルトは昇順(小さい→大きい、古い→新しい)   
逆にしたいときはdesc   
   
* T   
rails11の続き   
   
## 2025/10/22(Wed)
*YWT*   
* Y   
rails11   
   
* W   
・ownメソッド   
あるオブジェクトが特定のユーザーに属しているかどうかを判定するために作られたインスタンスメソッド   
→ユーザーオブジェクトと任意のオブジェクトを比較し、そのオブジェクトのuser_idがユーザーオブジェクトのidと一致するかどうかを確認   
   * `id == object&.user_id`   
   idは現在のユーザーオブジェクトのID   
   `object&.user_id`は与えられたオブジェクトのuser_idを取得   
   `&.（セーフナビゲーション演算子）`を使用することでobjectがnilの場合でもエラーを発生させずにnilを返す   
      
・ネスト   
入れ子構造（箱の中に箱が入っているイメージ）   
   
・shallowオブション   
親リソースが必要なアクションだけ親付きURLにして、それ以外はコメント単体（子リソース）だけでアクセスできるようにする   
   
・`.includes(.user)`   
コメントを取る時に、コメントしたユーザー情報も一緒に取っておく   
   
・`form_with`   
簡潔にフォームを作成でき、モデルに基づいたデータの送信ができる   
   
鬼門rails基礎１１に苦闘中。   
流れを整理して、なぜこの場所にこれを書くのか、これは何と繋がっているのかを意識して突破する   
   
* T   
rails基礎１１、１２

